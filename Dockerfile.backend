FROM rag-ingestion-base:latest

# 작업 디렉토리 설정 (Base Image에서 이미 설정되었지만 명시)
WORKDIR /app

# 소스 코드 복사 (Lean build)
COPY app/ ./app/

# FastAPI 실행
CMD ["uv", "run", "uvicorn", "app.interfaces.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
