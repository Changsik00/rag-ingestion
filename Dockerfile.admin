FROM rag-ingestion-base:latest

# 작업 디렉토리 설정
WORKDIR /app

# Admin 및 App 코드 복사 (Verification Lab 의존성 해결)
COPY admin/ ./admin/
COPY app/ ./app/

# Streamlit 실행
CMD ["uv", "run", "streamlit", "run", "admin/dashboard.py", "--server.address=0.0.0.0", "--server.port=8501"]
